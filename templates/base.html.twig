<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Default title{% endblock %}</title>
    <link rel="stylesheet" href="{{ asset('css/global.css') }}"> 
    {% block stylesheets %}{% endblock %}
    <style>
        /* Style pour le menu déroulant */
        .dropdown {
            position: relative;
            display: inline-block;
        }
        .dropdown-content {
            display: none;
            position: absolute;
            background-color: #f9f9f9;
            min-width: 160px;
            box-shadow: 0px 8px 16px 0px rgba(0,0,0,0.2);
            z-index: 1;
        }
        .dropdown-content a {
            color: black;
            padding: 12px 16px;
            text-decoration: none;
            display: block;
        }
        .dropdown-content a:hover {background-color: #f1f1f1;}
        .dropdown:hover .dropdown-content {display: block;}
    </style>
</head>
<body>
    <header>
        <div class="logo">LOP</div>
        <nav>
            <a href="{{ path('home') }}">Accueil</a>
            {% if not app.user %}
                <a href="{{ path('login') }}">Se connecter</a>
            {% else %}
                <div class="dropdown">
                    <button class="dropbtn">{{ app.user.username }}</button>
                    <div class="dropdown-content">
                        <a href="{{ path('profile_index') }}">Settings</a>
                        <a href="{{ path('app_logout') }}">Logout</a>
                    </div>
                </div>
                <a href="swipe">Finder</a>
            {% endif %}
            <a href="">À propos</a>
        </nav>
    </header>
    
    {% block body %}
    <!-- The content of your body will be defined in child templates -->
    {% endblock %}
    
    <footer>
        <div class="footer-content">
            <p>LOP - League Of Personality</p>
            <nav>
                <a href="">Paramètres des cookies</a>
                <a href="">Conditions d'utilisation</a>
                <a href="">Politique de confidentialité</a>
            </nav>
        </div>
    </footer>

    {% block javascripts %}
        <script src="{{ asset('public/js/dropdown.js') }}"></script>
    {% endblock %}
</body>
</html>
